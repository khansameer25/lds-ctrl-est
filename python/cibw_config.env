# Minimal cibuildwheel configuration

# Build these Python versions
CIBW_BUILD = "cp39-* cp310-* cp311-* cp312-*"

# Skip problematic builds
CIBW_SKIP = "*-win32 *-manylinux_i686 *-musllinux*"

# Use modern manylinux to avoid CentOS 7 issues
CIBW_MANYLINUX_X86_64_IMAGE = "manylinux_2_28"

# Simple system package approach - no vcpkg, add make
CIBW_BEFORE_ALL_LINUX = "dnf install -y cmake ninja-build gcc-c++ make openblas-devel hdf5-devel"
CIBW_BEFORE_ALL_MACOS = "brew install cmake ninja openblas hdf5"
CIBW_BEFORE_ALL_WINDOWS = "choco install cmake ninja"

# Build without vcpkg - override the toolchain file and disable fitting
CIBW_BEFORE_BUILD = "pip install pybind11 numpy && cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -DLDSCTRLEST_BUILD_PYTHON=ON -DLDSCTRLEST_BUILD_FITTING=OFF -DCMAKE_TOOLCHAIN_FILE= && cmake --build build --target python_modules"

# Test
CIBW_TEST_COMMAND = "python -c 'import ldsctrlest; print(\"Success!\")'"
